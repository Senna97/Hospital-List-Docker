spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/likelion-db
    username: root
    password: 1234

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  servlet:
    encoding:
      force-response: true